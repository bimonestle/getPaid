var options = {
    strings: ['<i>First</i> sentence.', '&amp; a second sentence.'],
    typeSpeed: 40,
    // loop: true,
    // loopCount: 4,
    showCursor: true,
    cursorChar: '|',
    autoInsertCss: true
  };
  
  var typed = new Typed('#terminal-typing', options);